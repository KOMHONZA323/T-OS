# T-OS
Build for the first time 
cmake -S . -B build

Build for T-OS
cmake --build build

Build with host run 
cmake --build build --target run

for host 

sudo dnf install cmake nasm xorriso qemu-system-x86
